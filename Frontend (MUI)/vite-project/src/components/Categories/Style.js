

export const useStyles = makeStyles((theme) => ({
    card: {
        width: 240,
        marginBottom: theme.spacing(2),
        cursor: 'pointer',
    },
    cardMedia: {
        height: 140,
    },
    categoryName: {
        textAlign: 'center',
        marginTop: theme.spacing(1),
    },
}));